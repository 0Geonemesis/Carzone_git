<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:j2seproject3="http://www.netbeans.org/ns/j2se-project/3"
         xmlns:jaxrpc="http://www.netbeans.org/ns/jax-rpc"
         basedir=".."
         default="default"
         name="CarZone-impl">
    <fail message="Please build using Ant 1.8.3 or higher." unless="ant.version"/>
    <property file="nbproject/private/private.properties"/>
    <property file="nbproject/project.properties"/>
    <property name="build.dir" value="build"/>
    <property name="dist.dir" value="dist"/>
    <property name="src.dir" value="src"/>
    <property name="test.src.dir" value="test"/>
    <property name="build.classes.dir" value="${build.dir}/classes"/>
    <property name="dist.jar" value="${dist.dir}/CarZone.jar"/>
    <property name="main.class" value="carzone.Main"/>

    <target name="default" depends="run" description="Build and run project."/>

    <target name="-init-macrodef-javac">
        <macrodef name="javac" uri="http://www.netbeans.org/ns/j2se-project/3">
            <attribute default="${src.dir}" name="srcdir"/>
            <attribute default="${build.classes.dir}" name="destdir"/>
            <attribute default="${javac.classpath}" name="classpath"/>
            <attribute default="${javac.compilerargs}" name="compilerargs"/>
            <sequential>
                <javac debug="true" deprecation="${javac.deprecation}"
                       destdir="@{destdir}" encoding="${source.encoding}"
                       includeantruntime="false" source="${javac.source}"
                       srcdir="@{srcdir}" target="${javac.target}">
                    <classpath>
                        <path path="@{classpath}"/>
                    </classpath>
                    <compilerarg line="@{compilerargs}"/>
                </javac>
            </sequential>
        </macrodef>
    </target>

    <target name="init" depends="-init-macrodef-javac">
        <mkdir dir="${build.classes.dir}"/>
        <mkdir dir="${dist.dir}"/>
    </target>

    <target depends="init" name="compile">
        <j2seproject3:javac srcdir="${src.dir}" destdir="${build.classes.dir}" classpath="${javac.classpath}"/>
    </target>

    <target depends="compile" name="jar">
        <jar compress="${jar.compress}" destfile="${dist.jar}" manifest="${manifest.file}">
            <manifest>
                <attribute name="Main-Class" value="${main.class}"/>
                <attribute name="Class-Path" value="${javac.classpath}"/>
            </manifest>
            <fileset dir="${build.classes.dir}"/>
        </jar>
    </target>

    <target depends="compile" name="run">
        <java classname="${main.class}" fork="true">
            <classpath>
                <path path="${run.classpath}"/>
            </classpath>
        </java>
    </target>

    <target name="clean">
        <delete dir="${build.dir}"/>
        <delete dir="${dist.dir}"/>
    </target>
</project>
